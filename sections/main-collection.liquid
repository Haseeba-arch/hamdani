<div class="main-collection-page section-padding">
  <div class="container">
    <div class="section-header-center">
      <h1 class="section-heading-main">{{ collection.title | default: 'All Baby Outfits' }}</h1>
      <p class="section-subtitle">{{ collection.description | default: 'Explore our cozy, organic and stylish baby & toddler collections.' }}</p>
    </div>
    {% section 'collection-tabs' %}
  </div>
</div>

{% schema %}
{
  "name": "Main Collection",
  "settings": []
}
{% endschema %}
